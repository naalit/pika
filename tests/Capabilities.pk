type DoNotCopy of
    New(I32)

type T struct
    x: DoNotCopy
    y: mut DoNotCopy
    z: own DoNotCopy

fun a(x: DoNotCopy) = ()
fun b(x: mut DoNotCopy) = ()
fun c(x: own DoNotCopy) = ()

fun f(t: mut T) = do
    own t2 = t
    mut q: I32 = 3
    q = 4
    a(t.x)
    b(t.y)
    # c(t.z)
    mut a: I32, b: I32 = 3, 4
    a = 3
    b = 4

fun p(t: (mut T, mut T) mut-> ()) = ()

fun q() = do
    p do x, y => # TODO should these variables require `mut` annotation? it's currently optional
        x.y = DoNotCopy.New(3)
        y.x = DoNotCopy.New(3)
